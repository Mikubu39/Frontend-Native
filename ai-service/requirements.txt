# Phụ thuộc để CHẠY THẬT (production).
#
# Chú ý danh sách này KHÔNG có torch / sentence-transformers. Chúng chỉ được
# dùng để ĐO ĐỐI CHỨNG lúc phát triển (xem requirements-dev.txt). Mô hình được
# đưa vào chạy thật chỉ cần scikit-learn, và đó chính là điều giữ cho ảnh
# triển khai đủ nhỏ để lọt gói hosting miễn phí 512 MB RAM.
#
# Tổng dung lượng cài đặt: ~120 MB (so với ~2.5 GB nếu kéo theo torch).

scikit-learn==1.9.0
numpy==2.5.2
scipy==1.18.1
joblib==1.5.3
PyYAML==6.0.3

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
