# Phụ thuộc để CHẠY THẬT (production).
#
# Danh sách này cố ý RẤT ngắn. Từ bản 2.0 phần hiểu ngôn ngữ do Gemini đảm
# nhiệm, nên toàn bộ scikit-learn / numpy / scipy (~120 MB) đã được gỡ bỏ.
# Lời gọi Gemini dùng `urllib` của thư viện chuẩn chứ không dùng SDK chính
# thức, vì SDK kéo theo grpcio + protobuf + google-auth mà ta chỉ cần đúng một
# lời gọi POST.
#
# Hệ quả đo được: ảnh triển khai ~15 MB thay vì ~140 MB, và dịch vụ tỉnh dậy
# sau khi ngủ đông trong vài giây thay vì ~30 giây.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
PyYAML==6.0.3
